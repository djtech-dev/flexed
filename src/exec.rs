pub fn exec(s: &str) -> bool {
    // 1. Split split
    // 2. Execute process
    // 3. Return boolean
}
pub fn track(s: &str) {
    // 1. Thread: Split string
    // 2. Thread: Execute&fork process
    // 3. Thread: Check if the process is alive, if it isn't run `track(s)`

    // 4. Run thread and make independent
}
